# Shared runtime
NODE_ENV=development
LOG_LEVEL=info
SERVICE_ROLE=arena
PORT=8080
DEMO_MODE=fake
DEMO_STEP_DELAY_MS=600
LOOP_MEMORY_DIRECTORY=.loop-memory
LOOP_READINESS_THRESHOLD=75
LOOP_MIN_HOSTILE_EVALUATIONS=4
LOOP_MIN_LEGITIMATE_CONTROLS=3
LOOP_MAX_EPISODES=8
LOOP_STAGNATION_EPISODES=3
LOOP_MAX_ZERO_SPEND_USD=1

# Live Zero adapter (required when ZERO_MODE=live)
ZERO_MODE=fake
ZERO_RUNNER=zero
ZERO_TIMEOUT_MS=30000
ZERO_ALLOWED_CAPABILITY_REFS=
ZERO_ALLOWED_TARGET_DOMAINS=
ZERO_TARGET_BASE_URL=
ZERO_MAX_PER_CALL_USD=0.05

# Sponsor/ATS sandbox gateway (required when RECRUITING_OPS_MODE=http)
RECRUITING_OPS_MODE=fake
OUTBOUND_RECRUITING_BASE_URL=
OUTBOUND_RECRUITING_BEARER_TOKEN=
OUTBOUND_RECRUITING_TIMEOUT_MS=10000

# Team-controlled Google Calendar sandbox (required when CALENDAR_MODE=google)
CALENDAR_MODE=memory
GOOGLE_CALENDAR_OAUTH_ACCESS_TOKEN=
GOOGLE_CALENDAR_SANDBOX_ID=
GOOGLE_CALENDAR_TIMEOUT_MS=10000
SANDBOX_CALENDAR_ATTENDEE_EMAIL=
SANDBOX_SCREEN_START_AT=
SANDBOX_SCREEN_END_AT=
SANDBOX_SCREEN_TITLE=[HACKATHON TEST] Screening
SANDBOX_SCREEN_DESCRIPTION=Evidence-backed screening event in the team-controlled sandbox calendar.

# Optional human-in-the-loop phone closure. Enable only after all values are configured.
ELEVENLABS_LOOP_CLOSURE_ENABLED=false
ELEVENLABS_API_KEY=replace-with-elevenlabs-api-key
ELEVENLABS_AGENT_ID=replace-with-elevenlabs-agent-id
ELEVENLABS_PHONE_NUMBER_ID=replace-with-elevenlabs-phone-number-id
ELEVENLABS_TO_NUMBER=+14155550123
# Optional: enables the faster signed post-call webhook path. Transcript polling works without it.
ELEVENLABS_WEBHOOK_SECRET=replace-with-elevenlabs-webhook-secret

# Private service authentication — replace locally; never commit real values
INTERNAL_AGENT_TOKEN=replace-with-at-least-24-random-characters
LOG_BRIDGE_TOKEN=replace-with-at-least-24-random-characters

# Internal recruiting services
ARENA_INTERNAL_URL=http://arena:8080
RECRUITING_MCP_INTERNAL_URL=http://recruiting-mcp:8084/mcp

# Pomerium Zero data plane bootstrap
# Keep real values only in .env; .env is gitignored.
POMERIUM_ZERO_TOKEN=replace-with-zero-cluster-token
POMERIUM_STARTER_DOMAIN=replace-with-starter-subdomain.pomerium.app

# Identity-scoped Service Accounts use the same protected route/audience.
SOURCER_MCP_URL=https://recruiting-mcp.example.invalid/mcp
VERIFIER_MCP_URL=https://verifier-mcp.example.invalid/mcp
CONTROLLER_MCP_URL=https://recruiting-mcp.example.invalid/mcp
SOURCER_POMERIUM_JWT=replace-with-sourcer-service-account-jwt
VERIFIER_POMERIUM_JWT=replace-with-verifier-service-account-jwt
CONTROLLER_POMERIUM_JWT=replace-with-controller-service-account-jwt
POMERIUM_JWKS_URL=https://recruiting-mcp.example.invalid/.well-known/pomerium/jwks.json
POMERIUM_ISSUER=https://authenticate.example.invalid
POMERIUM_AUDIENCE=recruiting-mcp.example.invalid
POMERIUM_SOURCER_SUBJECT=outbound-sourcer-service
POMERIUM_CONTROLLER_SUBJECT=hiring-controller-service

# Pin the sponsor-confirmed MCP-capable image digest before judging.
POMERIUM_IMAGE=pomerium/pomerium:latest
